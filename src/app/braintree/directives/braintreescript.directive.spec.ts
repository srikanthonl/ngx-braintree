import { BraintreescriptDirective } from './braintreescript.directive';

describe('BraintreescriptDirective', () => {
  it('should create an instance', () => {
    const directive = new BraintreescriptDirective();
    expect(directive).toBeTruthy();
  });
});
